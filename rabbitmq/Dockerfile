# Use official RabbitMQ image with management UI
FROM rabbitmq:3-management

# Set default credentials (you can override in Railway environment variables)
ENV RABBITMQ_DEFAULT_USER=admin
ENV RABBITMQ_DEFAULT_PASS=admin
